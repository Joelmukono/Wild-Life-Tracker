{{#partial "content"}}

    <h1> List of Animals</h1>

    {{#if animals}}
        {{#each animals }}
            <p>Current task:</p>
            <p>{{ name }} </p>
        {{/each}}
    {{else}}
        <p>No animals!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}